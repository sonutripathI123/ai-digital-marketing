# Optional / heavy dependencies.
#
# google-ads (with its grpcio + protobuf deps) is installed separately and
# "best-effort" so that a slow or memory-limited free-tier build never blocks
# the whole app from deploying. If it fails to install, the app still runs and
# the Google Ads live client reports "library not installed" via its status()
# check (falling back to clearly-labelled DEMO data) until it is present.
#
# --prefer-binary + a pinned Python 3.11 (see .python-version / runtime.txt)
# make pip use prebuilt wheels for grpcio instead of compiling from source,
# which is what caused the out-of-memory build failures on the free plan.
google-ads>=25.1.0
